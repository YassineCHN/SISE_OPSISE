services:
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sise_opsise_streamlit
    ports:
      - "8501:8501"
    env_file:
      - src/.env
    restart: unless-stopped
